<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.entertech.edevn">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/main_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EDEVN">
        <activity android:name=".SubjectDetailsActivity"></activity>
        <activity android:name=".LiveClassActivity" />
        <activity android:name=".ClassSelectedActivity" />
        <activity android:name=".OTPVerificationActivity" />
        <activity android:name=".LoaderVerificationActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoaderClassRoomActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ClassSelectActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>